# README

Research / trading-pilot workspace for ETHUSDT wave geometry, reversal timing, and futures execution studies.

## Research roadmap

The canonical project research sequence and the **current active / primary WIP** are maintained in:

**[`ROADMAP.md`](ROADMAP.md)**

Per-WIP reports live under [`docs/wip/`](docs/wip/).  
Do not treat chat history or ad hoc notes as roadmap authority.

### Current principle

**WHERE ≠ WHEN** — a calculated price area is not an entry signal.  
New strategy shape: `WHERE → WAIT → WHEN → ONE ENTRY`.

### Working tree note

Primary research/UI package currently lives under `phase3_staging/crypto_trading_bot/research_v2/`.  
Runtime artifacts for completed geometry WIPs are on S13 under `/var/tmp/traiding_pilot_ui_workspace/` (not committed).
