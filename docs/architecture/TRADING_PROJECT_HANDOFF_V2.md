# Trading project handoff V2

The project authority is Git at `origin/main`, currently
`a5816ad7a42c131fe8e38d95e96cdecafe5eddb1` before this WIP's changes. The
completed multi-timeframe composite search is frozen and closed. The active
follow-on builds only a causal training dataset for the development range.

S13 is compute/research/UI; S7 is canonical market-data and PostgreSQL
authority. See `RUNTIME_HOST_MAP_V2.md` and `OPERATIONS_RUNBOOK_V2.md` for the
current Tailscale routes. V1 documents are retained as historical evidence.
